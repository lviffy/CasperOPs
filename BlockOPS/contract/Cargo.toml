[package]
name = "blockops_contracts"
version = "0.1.0"
edition = "2021"

[dependencies]
odra = "2.8.1"

[dev-dependencies]
odra-test = "2.8.1"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
